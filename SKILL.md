---
name: private-mcp-development
description: 비공개·조직 내부용 MCP 서버를 새로 설계하거나 구현, 검토, 변경, 릴리스할 때 사용한다. 목적·프로토콜 호환성·신원·객체 인가·데이터 경계·도구 계약·쓰기 안전성·장애 복구·검증 증거를 공통 절차로 점검하고 설계 기록과 인수 시험을 작성한다.
compatibility: 파일 기반 Agent Skills를 읽는 호스트에서 사용한다. 문서만 읽을 때는 실행 의존성이 없다. 선택적 기록 구조 검사기는 Python 3.10 이상 표준 라이브러리만 사용한다.
metadata:
  version: "0.1.0"
  evidence-date: "2026-09-10"
  status: "research-backed-draft"
---

# Private MCP 개발

## 적용 범위와 경계

Private MCP는 별도 프로토콜이 아니라 접근·배포·네트워크·데이터 범위를 통제하는 MCP 시스템으로 해석한다. 이 스킬은 개발 절차이며 인증·인가·승인·격리의 실행 경계가 아니다. `allowed-tools` 같은 호스트 메타데이터나 자연어 지시만으로 서버 보안이 강제된다고 주장하지 않는다.

16개 통제 영역은 모두 검토하되 기능은 조건에 따라 구현한다. 불필요한 OAuth, DB, 게이트웨이, Tasks, UI를 일률적으로 추가하지 않는다. 공통 정책 제안과 선택한 사양의 MUST/SHOULD/MAY를 구분한다.

## 먼저 읽을 자료

기준 선택: [프로토콜·배포 프로파일](references/protocol-profiles.md)

통제 정의: [C01–C16](references/security-controls.md)

시험 설계: [T01–T36](references/validation.md)

실제 사례: [사례와 해석](references/case-studies.md)

근거 확인: [출처 목록](references/research-evidence.md)

## 1. 목적과 현재 상태를 확인한다

사용자 과제, 비목표, 소유자, 호출 주체, 보호 자원, 읽기·쓰기·외부 전송·코드 실행을 기록한다. 프로젝트에 접근 권한이 있으면 제공된 코드·설정·배포 자료를 먼저 읽고, 공개 예시와 실제 프로젝트를 혼동하지 않는다.

MCP가 필요한 이유와 기존 API/CLI/스킬 대안을 비교한다. 확인되지 않은 IdP, SDK, 네트워크, 역할, 기존 승인, 실행 결과를 만들어내지 않는다. 답이 없는 필드는 미정으로 남긴다.

산출물: [설계 기록](assets/design-review-template.md)의 범위·가정·미정 항목.

## 2. 호환성과 프로파일을 선택한다

공식 날짜별 사양과 지원할 호스트·SDK를 확인한다. 이 패키지의 근거일에는 최신 포인터가 2026-07-28로 연결되었지만, 사용 시점의 최신·지원 상태는 다시 확인한다.

Modern / Legacy / Dual-era, stdio / HTTP, 사용자 위임 / 서비스 신원, 단일 사용자 / 멀티테넌트, 쓰기·실행·상태의 적용 여부를 결정한다. 최신이라는 이유만으로 운영 호스트의 지원을 추정하지 않는다.

산출물: [호환성 표](assets/compatibility-matrix-template.md), 명시적 폴백·거부 정책.

## 3. 위협 모델과 강제 위치를 정한다

데이터가 원본·서버·호스트·모델·외부 목적지·로그·캐시·백업으로 흐르는 경로를 기록한다. 공격자가 바꿀 수 있는 도구 설명, 외부 문서, 인수, URL, 파일, 요청 상태를 표시한다.

C01–C16마다 적용 판단, 책임자, 구현 위치, 메커니즘과 검증 증거를 정한다. 프롬프트를 통제 위치로 적는 것만으로 인가·승인·반출 통제를 완료 처리하지 않는다.

검증 신원은 인증 문맥 또는 명시적 로컬 경계에서 도출한다. `role_id`, `tenant_id`, `clientInfo`, `confirmed: true`, 도구 annotation과 불투명 핸들만을 신원·권한·승인 증거로 사용하지 않는다.

## 4. 도구 계약을 먼저 작성한다

[도구 계약 양식](assets/tool-contract.template.json)을 도구별로 채운다. 이 양식은 설계 산출물이며 MCP wire schema가 아니다.

목적·비목적, 입력·출력 의미, 권한, 부작용, 승인, 멱등성, 시간·출력·비용 한도, 오류·부분 성공·불명확 결과, 상태·캐시·보존, 감사와 시험을 기록한다. 적용하지 않는 기능은 이유를 쓴다.

API 전체를 그대로 노출하기보다 대표 과제에 필요한 최소 작업을 선택한다. 출력의 출처·페이지·절단 의미를 유지한다. annotation은 집행 수단이 아니다.

## 5. 실행 경계에서 통제를 구현한다

MCP 어댑터, 정책/도메인 실행기, 하위 서비스·OS 권한의 책임을 나눈다. 목록 필터와 직접 호출 인가를 별도로 검사한다. MCP 토큰과 하위 API 토큰의 대상을 분리한다.

위험 쓰기는 실제 승인 또는 제한된 사전 승인 정책을 검증한다. 승인 후 인수 변경, 병렬 중복, 응답 유실 뒤 재시도, 대상 버전 충돌을 처리한다. JSON-RPC ID를 멱등성 키로 오인하지 않는다.

업무 핸들을 프로토콜 세션과 분리한다. MRTR 상태는 의미에 맞는 무결성·주체·인수 결속과 필요 시 일회성을 강제한다. 취소를 외부 변경의 롤백 완료로 보고하지 않는다.

비신뢰 텍스트가 실행·반출 정책을 변경하지 못하도록 최소 권한, 허용 목적지·파일·실행 범위, 격리를 적용한다. 정상적인 등록 내부 서비스와 임의 내부 목적지를 구분한다.

## 6. 검증을 실행하고 증거를 기록한다

[시험 카탈로그](assets/acceptance-tests.json)에서 적용 여부를 판단하고 업무별 시험을 추가한다. 프로토콜 정합성, 결정론적 정책, 장애·복구, 실제 호스트·모델 과제, 적대적 다중 도구 조합을 분리한다.

합성 데이터와 승인된 격리 환경을 사용한다. 파괴적 작업·외부 공개 전송·비용 유발을 문서 작성 권한만으로 실행하지 않는다. 모델·호스트·SDK·사양·도구 설명·정책·fixture 버전을 남긴다.

Conformance의 종료 코드만 보지 말고 버전별 적용 시험, expected failure, skip, not_scored를 검토한다. 미실행을 통과로 바꾸지 않는다. 확률적 평가의 제한도 기록한다.

## 7. 릴리스와 변경을 검토한다

[릴리스 양식](assets/release-gate-template.md)으로 목적·계약·구현·시험·운영 게이트를 검토한다. 비밀 회수, 도구 차단, 감사와 복구 책임을 확인한다.

예외는 사유·영향·보완책·승인자·만료를 기록한다. 사양 MUST 위반을 내부 승인으로 준수 처리하지 않는다. 도구 설명, 권한, 의존성, 호스트, SDK, 데이터 등급 변경도 재검증 대상으로 본다.

## 선택적 기록 구조 검사

프로젝트 기록은 [빈 JSON 양식](assets/project-manifest.template.json)을 복사해 작성한다. 패키지 루트에서 다음을 실행할 수 있다.

```bash
python scripts/validate_artifacts.py path/to/project-manifest.json
python scripts/validate_artifacts.py path/to/project-manifest.json --release-record
python -m unittest discover -s tests -v
```

첫 명령은 구조를, 두 번째는 제출된 릴리스 기록의 필드 완결성을 검사한다. 실제 서버 호출·인증 검증·증거 진위 확인·보안 시험은 하지 않는다. 세 번째는 검사기 자체 시험일 뿐이다. 상세 범위는 README를 따른다.

## 실행·릴리스 중단 조건

보호 작업의 신원·객체 인가를 검증할 수 없을 때, 고위험 승인 증거가 요청 인수뿐일 때, 민감 데이터의 임의 반출 경로가 남아 있을 때, 결과 불명확인 쓰기를 무조건 재실행하려 할 때 해당 작업을 중단한다.

필수 시험 실패·미실행, 미지원 호환 조합, 만료 예외, 사실과 다른 성공 기록을 릴리스 승인으로 바꾸지 않는다. 위험한 실행은 중단하되 안전한 설계·문서화·결함 분석은 계속할 수 있다.

## 최종 제출

설계 기록, 버전·호스트 호환성 표, 도구 계약, C01–C16 판단, 적용 T01–T36 결과·증거, 예외·미정 항목, 릴리스·차단·복구 기록을 함께 제출한다. 실제로 수행한 작업과 제안·미실행 항목을 명시한다.
